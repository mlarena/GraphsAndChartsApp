@model GraphsAndChartsApp.ViewModels.SensorViewModel
@{
    var sensorId = Model.Id;
}
<div class="card bsp-chart-sensor-card" id="dovContainer" data-sensor-id="@sensorId">
    <div class="card-header bg-info text-white">
        <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="fas fa-eye"></i> Датчик оптической видимости (DOV)</h5>
            <span class="badge bg-light text-dark"><i class="fas fa-barcode"></i> @Model.SerialNumber</span>
        </div>
    </div>
    <div class="card-body">
        <div class="row mb-3">
            <div class="col-md-12">
                <div class="d-flex justify-content-between align-items-center bg-light p-2 rounded">
                    <div><i class="fas fa-map-marker-alt text-info"></i> <span class="fw-bold">@Model.EndPointsName</span></div>
                    <div><i class="fas fa-building text-info"></i> <span>@Model.MonitoringPostName</span></div>
                    <div><i class="fas fa-clock text-info"></i> <span id="lastUpdateTime">Загрузка...</span></div>
                    <div class="d-flex align-items-center ms-3 bsp-chart-auto-update-control" id="autoUpdateControl">
                        <div class="form-check form-switch d-inline-block m-0">
                            <input class="form-check-input bsp-chart-form-check-input" type="checkbox" id="autoUpdateToggle" role="switch" checked>
                            <label class="form-check-label small fw-normal ms-1" for="autoUpdateToggle"><i class="fas fa-sync-alt fa-xs"></i> 30с</label>
                        </div>
                        <span id="countdownTimer" class="badge bg-secondary ms-2 bsp-chart-countdown-timer">30</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-md-12">
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                        <span class="text-muted me-2"><i class="fas fa-chart-pie"></i></span>
                        <select id="chartTypeSelect" class="form-select form-select-sm" style="width: auto;">
                            <option value="visibility" selected>📈 Дальность видимости</option>
                            <option value="brightness">☀️ Освещенность (bright_flag)</option>
                        </select>
                    </div>
                    <div class="d-flex align-items-center">
                        @await Html.PartialAsync("_TimeRangePartial", new { Prefix = "dov", OutlineColor = "primary", ActiveDays = 1 })
                    </div>
                </div>
            </div>
        </div>
        @await Html.PartialAsync("_DateRangeSectionPartial", new { Prefix = "dov", Color = "primary" })
        <div id="chartsContainer">
            <div class="card">
                <div class="card-header bg-light py-2 d-flex justify-content-between align-items-center">
                    <h6 class="mb-0"><i class="fas fa-chart-line text-info"></i> <span id="chartTitle">Дальность видимости (метры)</span></h6>
                    <div class="d-flex align-items-center"><span class="text-muted small me-2">Шкала:</span><span id="timeScaleLabel" class="badge bg-secondary">авто</span></div>
                </div>
                <div class="card-body p-2">
                    <canvas id="visibilityChart" class="bsp-chart-canvas" style="height: 450px; width: 100%;"></canvas>
                    <canvas id="brightnessChart" class="bsp-chart-canvas" style="height: 450px; width: 100%; display: none;"></canvas>
                </div>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col-md-12">
                <div class="d-flex justify-content-around bg-light p-2 rounded">
                    <div class="text-center"><div class="small text-muted">Мин. видимость</div><div class="fw-bold" id="minVisibility">-</div><div class="small">м</div></div>
                    <div class="text-center"><div class="small text-muted">Макс. видимость</div><div class="fw-bold" id="maxVisibility">-</div><div class="small">м</div></div>
                    <div class="text-center"><div class="small text-muted">Средняя</div><div class="fw-bold" id="avgVisibility">-</div><div class="small">м</div></div>
                    <div class="text-center"><div class="small text-muted">Измерений</div><div class="fw-bold" id="totalMeasurements">-</div><div class="small">шт</div></div>
                </div>
            </div>
        </div>
    </div>
</div>
<div id="chartLoadingIndicator" class="text-center my-2 bsp-chart-loading-indicator" style="display: none;">
    <div class="spinner-border spinner-border-sm text-primary" role="status"><span class="visually-hidden">Загрузка...</span></div>
    <span class="ms-2 text-muted small">Обновление данных...</span>
</div>
<script>
    $(document).ready(function() {
        if (typeof DOVCharts !== 'undefined') {
            if (DOVCharts.cleanup) DOVCharts.cleanup();
            DOVCharts.init(@sensorId);
            $(document).trigger('sensorChanged');
        } else { console.error('DOVCharts не загружен!'); }
    });
</script>